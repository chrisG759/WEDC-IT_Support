<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Signup</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh; /* Full height */
            margin: 0; /* Remove default margin */
            background-color: #f4f4f4; /* Light background */
        }

        .signup-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            border: 1px solid black;
            width: 320px; /* Box width */
            padding: 20px;
            background-color: #ff8e31;
            color: white;
            text-align: center;
            border-radius: 10px; /* Rounded corners */
            box-shadow: 5px 5px 15px rgba(0, 0, 0, 0.2); /* Shadow */
        }

        .signup-form table {
            margin: auto;
        }

        input[type="text"], 
        input[type="password"] {
            width: 100%;
            padding: 5px;
            margin: 5px 0;
        }

        input[type="submit"] {
            background-color: white;
            color: #ff8e31;
            border: none;
            padding: 8px 15px;
            cursor: pointer;
            font-weight: bold;
            margin-top: 10px;
            border-radius: 5px;
        }

        input[type="submit"]:hover {
            background-color: #ffa863;
        }

        .login-link {
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="signup-container">
        <h1>Sign Up</h1>
        <div class="signup-form">
            <form action="/studentSignup" method="post">
                <table>
                    <tr>
                        <td><label for="studentEmail">Student Email: </label></td>
                        <td><input type="text" id="studentEmail" name="studentEmail"></td>
                    </tr>
                    <tr>
                        <td><label for="password">Password: </label></td>
                        <td><input type="password" id="password" name="password"></td>
                    </tr>
                    <tr>
                        <td><label for="confirmPassword">Confirm Password: </label></td>
                        <td><input type="password" id="confirmPass" name="confirmPassword"></td>
                    </tr>
                </table>
                <input type="submit" value="Sign Up">
            </form>
                <% if (signupError)%>
                    <p style="color: red;"><%= signupError %></p>

                 
        </div>
        <div class="login-link">
            <p>Have an account? <a href="/" style="color: white; text-decoration: underline;">Login</a></p>
        </div>
    </div>
</body>
</html>
