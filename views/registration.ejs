<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login/Signup</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh; 
            margin: 0; 
            background-color: #f4f4f4; 
        }

        .login {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            border: 1px solid black;
            width: 320px; 
            padding: 20px; 
            background-color: #ff8e31;
            color: white;
            text-align: center;
            border-radius: 10px; 
            box-shadow: 5px 5px 15px rgba(0, 0, 0, 0.2); 
        }

        .login-form table {
            margin: auto; 
        }

        input[type="text"], 
        input[type="password"] {
            width: 100%; 
            padding: 5px;
            margin: 5px 0;
        }

        input[type="submit"] {
            background-color: white;
            color: #ff8e31;
            border: none;
            padding: 8px 15px;
            cursor: pointer;
            font-weight: bold;
            margin-top: 10px;
            border-radius: 5px;
        }

        input[type="submit"]:hover {
            background-color: #ffa863;
        }
        #signup-link, a{
            color: white;
        }
    </style>
</head>
<body>
    <div class="login">
        <h1>Login</h1>
        <div class="login-form">
            <form action="/login" method="POST">
                <table>
                    <tr>
                        <td><label for="studentid">Email: </label></td>
                        <td><input type="text" id="studentEmail" name="email"></td>
                    </tr>
                    <tr>
                        <td><label for="password">Password: </label></td>
                        <td><input type="password" id="studentPassword" name="password"></td>
                    </tr>
                </table>
                <div>
                    <input type="submit">
                </div>
            </form>
            <% if(loginError) {%>
                <p style="color: red;"><%=  loginError%></p>
            <%} else if (accountCreated) { %>
                <p style="color: greenyellow;"><%= accountCreated %></p>
            <% } %>
        </div>
    
        <div id="signup-link">
            <p>Do not have an account? <a href="/signup">Signup</a></p>
        </div>
    </div>
</body>
</html>